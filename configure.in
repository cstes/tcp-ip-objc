dnl autoconf configure script for tcp-ip-objc

AC_INIT()

AC_CONFIG_AUX_DIR(build-aux)
AC_CANONICAL_SYSTEM

CPU_VENDOR_OS=$target
AC_SUBST(CPU_VENDOR_OS)

MFLAGS="-q -O"
AC_SUBST(MFLAGS)

AC_OUTPUT(Makefile config.h)

